[project]
name = "01-docker-terraform"
version = "0.1.0"
description = "Sample Lab for 01-docker-terraform"
requires-python = ">=3.13.11"
dependencies = [
    "pandas>=3.0.3",
    "pyarrow>=24.0.0",
]
